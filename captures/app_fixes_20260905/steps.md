# Build 13 review fixes — on-phone evidence (Moto g05 MOTO_SERIAL, Android 15, watch absent, phone Bluetooth off)

Build: `ryzeapp/app/build/outputs/apk/debug/app-debug.apk` md5 85ca9e59f49c03b34cc5fe32dc69f720, installed 10:47:10
(`tools/app_smoke.sh --no-build 25` → `captures/app_smoke_20260905_104700/`, no crash). Unit tests: 324, 0 failures
(`build_and_tests.log`; 307 before). Every adb step below was scripted; the phone's Bluetooth was never touched.

| Item | What was done | Evidence |
|---|---|---|
| Settings permission gate | All 7 granted → "Permissions granted". `pm revoke WRITE_EXERCISE_ROUTE` (kills the process), relaunch → host logs `export allowed, optional missing [WRITE_EXERCISE_ROUTE]`; card reads "Permissions granted (GPS routes not allowed: workouts are exported without their track)", button "Grant route permission" enabled, **"Export now" enabled** (uiautomator: clickable View enabled=true). `pm grant` back, resume → "Permissions granted", "Grant permissions" disabled. | `health_gate.txt`, `hc_all_granted.png`, `hc_route_revoked.png`, `hc_route_regranted.png` |
| Export toast / ledger | "Export now" twice: snackbar "Nothing new to export: 230 records already in Health Connect"; status "Last export 10:53: nothing new (230 records unchanged)". Logcat run 1: `0 sessions unchanged without reading their tracks` (companions missing → track read once, stored via markers), run 2: `1 sessions unchanged without reading their tracks`. Ledger afterwards holds `workout-1.base` and `workout-1.route = 86`. | `health_gate.txt`, `hc_export_now_toast_1.png`, `db_downgrade.txt` (ledger companions) |
| MAC field | `input text abcdefabcdef` ×3 → field reads exactly `abcdefabcdef` (the pre-fix field lost characters: `ABCDEFBCEF`). Real MAC typed in lower case → Save greyed (normalised value equals the stored one). `aa:bb:cc:dd:ee:ff` + Save → field re-seeds to `AA:BB:CC:DD:EE:FF`, WatchService logs the address change; default MAC typed in lower case + Save → DataStore `watch_mac = 78:02:B7:37:91:E5`. | `mac_field.txt`, `mac_field_lowercase_typed.png`, `mac_field_lowercase_real_mac.png`, `mac_field_after_save_upper.png`, `mac_field_restored.png` |
| Find-phone generations + DUMP guard | Merged manifest: receiver carries `android:permission="android.permission.DUMP"` (`manifest_debug_receiver.txt`). adb broadcasts still work: `ringing for up to 30 s (ring 1)` → stopped (watch) → `(ring 2)` → stopped. **Not done:** a third-party-uid negative test — `run-as` sends as the app's own uid, which Android always lets through (`checkComponentPermission` same-uid rule), and installing another app is not approved. That attempt started ring 3, stopped after 23 s. | `findphone.txt`, `findphone_ringing.png` |
| Workout titles | Workout tab list row and detail title/card: "Outdoor Walking · Sat 5 Sep 08:35" for the type-1 155 m walk (was "Outdoor Running"), matching the Health Connect session title. | `workout_titles.txt`, `workout_list_outdoor_walking.png`, `workout_detail_outdoor_walking.png` |
| Height → stride export | Profile height H → H+1, Save: `Health Connect export after stride change (walk 0.7503 m, run 0.9992 m): … distance=3, inserted=3`; back to 182: another `distance=3`. Height left at 182. | `height_export.txt` |
| Room without destructive fallback | Force-stop, push a checkpointed copy with `user_version = 4`, launch → `FATAL EXCEPTION … IllegalStateException: A migration from 4 to 3 was required but not found` (RoomOpenHelper.onDowngrade) — loud, no tables emptied. Restored the complete v3 copy (233 `hc_export` rows, companions, cursor 1788569953613), relaunch: no crash, dashboard shows 8,000 steps / 0.12 km. Note: the first push attempt failed on shell quoting and its `rm` of the `-wal` file dropped 4 un-checkpointed ledger rows; the restore from the checkpointed copy put them back (verified by row counts). | `db_downgrade.txt`, `db_downgrade_launch.png`, `db_restored_dashboard.png` |
| Missing stride marker | Not reproducible on this phone (the marker exists since build 9); covered by `HealthConnectExportPlannerTest.missingStrideMarkerOnAnIncrementalRunRebuildsEveryDay`. | `build_and_tests.log` |
| APP.md mapping nit | HeartRateRecord "grouped per epoch hour". | `docs/APP.md` |

Phone left as found: app running, all 7 Health Connect permissions granted, MAC 78:02:B7:37:91:E5, height H, no ring, database restored (see above).
