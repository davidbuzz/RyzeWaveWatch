<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="au.buzz.ryzebridge"
    android:versionCode="1"
    android:versionName="0.1">

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="34" />

    <!-- BLE (runtime-granted via: adb shell pm grant au.buzz.ryzebridge android.permission.BLUETOOTH_CONNECT) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- foreground service so the GATT connection survives between adb commands -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application android:label="RyzeBridge" android:allowBackup="false">
        <!-- headless entry point, started from adb (am start ... CmdActivity with a "script" string extra) -->
        <activity android:name=".CmdActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay" />
        <service android:name=".BleService"
            android:exported="true"
            android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
